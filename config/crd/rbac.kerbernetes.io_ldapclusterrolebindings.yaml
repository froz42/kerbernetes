---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: ldapclusterrolebindings.rbac.kerbernetes.io
spec:
  group: rbac.kerbernetes.io
  names:
    kind: LdapClusterRoleBinding
    listKind: LdapClusterRoleBindingList
    plural: ldapclusterrolebindings
    singular: ldapclusterrolebinding
  scope: Cluster
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: LdapClusterRoleBinding is the Schema for ldapclusterrolebindings
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              clusterRoleRef:
                properties:
                  name:
                    type: string
                required:
                - name
                type: object
              ldapGroupDN:
                type: string
            required:
            - clusterRoleRef
            - ldapGroupDN
            type: object
        required:
        - metadata
        - spec
        type: object
    served: true
    storage: true
